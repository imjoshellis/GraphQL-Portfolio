{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm install && npm run clean && tsc",
    "build:watch": "npm run clean && tsc-watch",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "apollo-server-lambda": "^2.18.1",
    "axios": "^0.20.0",
    "graphql": "^15.3.0"
  },
  "devDependencies": {
    "@types/node": "^13.11.0",
    "rimraf": "^3.0.2",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3"
  }
}
